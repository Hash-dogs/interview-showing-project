# 简历上传
## 一、上传整体步骤：
```angular2html
前端上传文件
    ↓
① 基础合法性校验（最先做，最便宜）
   - 文件大小是否超限
   - 文件后缀是否允许
   - 直接拒绝明显不合法的，不浪费后续资源
    ↓
② SHA-256 哈希计算 + 去重检查(不使用文件名和直接差rustfs是否有重复，这样比较耗时间。)
   - 计算哈希值
   - 查数据库是否已存在
   - 已存在 → 直接返回，流程结束
   - 这步在 Tika 之前，避免重复文件浪费解析资源
    ↓
③ Tika 解析（比较耗资源，放后面）
   - 提取文本内容
   - 提取元数据（作者、页数、创建时间等）
   - 验证文件真实类型（防止后缀名造假）
    ↓
④ 文本清洗
   - TextCleaningService.cleanText()
   - 去掉噪音、规范格式
    ↓
⑤ 存储
   - 上传文件本体到 RustFS → 拿到 fileKey
   - 数据库存入元信息
     ├── fileKey（RustFS路径）
     ├── fileHash（SHA-256，用于去重）
     ├── 元数据（Tika提取的作者、页数等）
     └── 清洗后的文本（用于后续AI/RAG分析）
    ↓
⑥ 返回结果给前端
```
# 简历分析

## 一、 从各类文件中提取文本 Apache Tika
1. 一个非常成熟的开源"内容分析工具箱"，它最大的特点是能从上千种不同格式的文件 （如PDF、Office文档、音频、视频等）中，通过统一接口提取出文本和元数据（Metadata）
2. 主要功能是做文本转换，对于文本的噪声处理功能十分有限，只有下面提及几个：
    * **能识别文件类型**（"application/pdf" "image/png"），格式和HTTP请求中的类型描述几乎一致
    * 能根据字符等单位截断文本（如只获取前10000个字符文本）
    * 语言检测（十分不稳定）


## 二、 简历简单除噪：
* TextCleaningService.cleanText()：使用正则表达式对TIka转化后的噪音进行除噪。





## 三、简历评估结果规范化：对大模型输出的json字符串解析解析和验证
1. 工具包提供BeanOutputConverter提供convert函数将大模型中内容进行了初步解析
```angular2html
content 字符串
     │
     ① 剥离 Markdown 代码块（```json ... ```）
     │
     ② 提取纯 JSON 字符串（本质是Jackson解析json代码语法分析）
     │
     ③ Jackson ObjectMapper.readValue() 反序列化：将纯json数据转化为对应DTO类的对象
     │
     ▼
ResumeAnalysisResponseDTO 对象
```
* 由于大模型可能不完全按照prompt输出，比如json添加了一些媒体及的数据或描述、某个字段的数据超过DTO中规定的范围等等。需要二次手动校准


2. 非法转义字符解析校准
* 本质：大模型可能会不加转义字符直接输出一些非法句子："他说"你好"然后离开了"。这里的双引号没转义就会被认为是字段的结束符号二错义
* 使用有限状态机思想解决：两个bool类型字段inString表示是否进入json字段，escape字段表示当前字符是否会被转义。
```
    private String repairUnescapedQuotesInJsonStrings(String content) {
        if (content == null || content.isBlank()) {//判断是否为null串以及空白字符串
            return content;
        }
        StringBuilder repaired = new StringBuilder(content.length() + 16);//由于要加如\作转义字符所以会比原串多
        boolean inString = false;
        boolean escaping = false;
        for (int i = 0; i < content.length(); i++) {//逐个字符扫描处理
            char ch = content.charAt(i);
            //当不是inString的时候遇到“说明是起始的“
            if (!inString) {
                if (ch == '"') {
                    inString = true;
                }
                repaired.append(ch);
                continue;
            }
            //说明上一个字符是转义字符
            if (escaping) {
                repaired.append(ch);
                escaping = false;
                continue;
            }
            //原串遇到\转义字符的时候下一个字符直接输出就好
            if (ch == '\\') {
                repaired.append(ch);
                escaping = true;
                continue;
            }
            if (ch == '"') {
            //算法关键所在：判断下一个字符是否为终结符（，] } ;）出错点常在这，判断结束符号不正确导致改错失败
                if (isLikelyJsonStringTerminator(content, i + 1)) {
                    inString = false;
                    repaired.append(ch);
                } else {
                    //append函数中也需要转移，实际添加的是\\和\",到了json原串中就是\"
                    repaired.append("\\\"");
                }
                continue;
            }
            repaired.append(ch);
        }
        return repaired.toString();
    }
```



